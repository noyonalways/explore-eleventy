---
title: Blogs
layout: "base.njk"
---

<section class="py-10">
  <div class="container">
    <h1 class="text-4xl font-bold mb-8">Recent Blogs</h1>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {%- for blog in collections.blog | reverse -%}
      <div
        class="bg-white dark:bg-gray-800 rounded-lg shadow-md p-6 transition-transform transform hover:scale-[0.98] cursor-pointer">
        <h2 class="text-2xl font-semibold mb-2">{{ blog.data.title }}</h2>
        <p class="text-gray-700 dark:text-gray-300 mb-4">{{ blog.data.description }}</p>
        <a href="{{ blog.url }}" class="text-primary font-semibold hover:underline">Read More</a>
      </div>
      {%- endfor -%}
    </div>
  </div>
</section>